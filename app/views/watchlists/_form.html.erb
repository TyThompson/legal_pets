<h4>Enter information manually</h4>
<%= bootstrap_form_for @watchlist, url: "/watchlists/#{@watchlist.id}" do |f| %>
<% if @watchlist.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@watchlist.errors.count, "error") %> prohibited this subject from being saved:</h2>

    <ul>
    <% @watchlist.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
 <%= f.text_field :species, label: "Species:" %>
 <%= f.number_field :price, step: 0.01, label: "Set Price:" %>
 <%= f.hidden_field :user_id, value: current_user.id %>
 <button class="btn btn-primary btn-sm">Submit</button>
<% end %>
